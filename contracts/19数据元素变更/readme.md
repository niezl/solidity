1. 数组的删除操作： 。在Solidity中,使⽤ delete 操作清除数组中的元素并不会真正删除该元素，⽽是将其重置为
默认值。
2. 数组元素的移除与数组缩减⽅法： 。介绍⼀种⽅法 ，通过左移数组中的元素并删除数组的最后⼀个元素来移除特定位置的元素并缩
减数组⻓度。
3. 实际操作演⽰： 。例如，从数组 [1, 2, 3 ] 中移除索引为1的元素(即数字2),操作⽅法是将索引1后的所有
元素左移，然后删除数组末尾的元素，得到结果 [1, 3 ] 。 。进⼀步解释：对于数组 [1, 2, 3, 4, 5, 6 ] , 移除索引2的元素(即数字3),移动索引2
右侧的所有元素(4, 5, 6)向左，然后删除最后⼀个元素，最终数组变为
4. 边界情况处理： 。如果数组只有⼀个元素且需要删除，操作后数组将变为空。
5. 算法实现： 。确保要删除的索引⼩于数组⻓度。 。使⽤循环从要删除的索引位置开始，将右侧的元素向左复制。 。循环完成后删除数组的最后⼀个元素 ，完成数组的缩减。
6. 测试验证： 。编写测试⽤例验证算法正确性，确保数组元素和⻓度符合预期结果。