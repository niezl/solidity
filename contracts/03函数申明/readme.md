1. 函数基本语法：介绍如何在 Solidity 中编写函数的基础语法，包括使⽤关键字 function 开始声明，
后接函数名和参数列表。
2. 输⼊和输出:通过两个例⼦(加法和减法函数)展⽰函数如何接收输⼊(uint x 和 uint y)并返回
计算结果。这些函数被定义为 external 和 pure,即它们可以从合约外部调⽤，并且不会修改区块
链上的数据。
3. 关键字解释：
• external:指该函数可以在部署合约后从外部被调⽤。
• pure:指该函数不会对区块链的状态进⾏写操作，仅⽤于执⾏读取计算。
4. 编译与部署：介绍如何编译 Solidity 合约（使⽤ Solidity 编辑器的快捷键 Ctrl+S 或点击编译标
签），以及如何部署合约以便调⽤其中的函数。
5. 函数调⽤⽰例：展⽰如何在合约部署后调⽤ add 和 sub 函数，包括输⼊参数和预期的输出结果。
6. 编程作业：
  具体要求如下：
• 定义⼀个名为 Calculator 的合约。
• 创建两个函数:multiply 和 divide。
◦ multiply 函数接收两个 uint 类型的参数，返回它们的乘积。
◦ divide 函数接收两个 uint 类型的参数，返回它们的商（注意处理除以零的情况）。
• 两个函数都应该使⽤ external pure 修饰符。
• 编译并部署合约，测试两个函数确保它们按预期⼯作。