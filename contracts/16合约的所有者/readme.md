1. 概念介绍：
状态变量：在合约中声明并使⽤状态变量（例如，合约的所有者）。
全局变量：如 msg.sender ，⽤于引⽤调⽤者地址。
函数修饰符：创建修饰符来限制函数调⽤者必须是合约的所有者。
函数：编写和部署合约中的函数。
错误处理：使⽤ require 语句来验证条件并处理错误情况。
2. 合约设计：
初始化所有者：在构造函数中，将部署合约的⽤⼾设置为所有者。
所有权管理：
▪ 创建⼀个 onlyOwner 修饰符，确保只有当前所有者可以调⽤特定函数。
▪ 设计⼀个 setOwner 函数，允许当前所有者转让所有权，前提是新所有者地址不为零。
3. 合约功能：
公开函数：开发可以由任何⼈调⽤的函数，以及仅限所有者调⽤的功能。
所有权转移：实现⼀个功能，允许安全地更改合约的所有者。
4. 合约部署与演⽰：
编译和部署：说明如何编译合约并使⽤特定账⼾部署。
功能验证：演⽰所有者和⾮所有者账⼾调⽤权限不同的函数，展⽰权限控制效果。